# 工作区杂乱度复盘（2026-02-24）

## 范围
- 仓库：`/Users/will/Desktop/通往AGI之路`
- 目标：给出“可直接删除”与“结构调整”决策依据。

## 现状快照
- 文件总量（不含 `.git`）：`8641`
- Git 跟踪文件：`342`
- 当前未提交改动：仅 `D .claude/worktrees/phase3-interaction`

### 顶层体量与文件数
- `文案部`：`321M` / `8427` 文件（绝对主噪声源）
- `公司业务`：`38M` / `165` 文件
- `日常vibe`：`868K` / `28` 文件
- `workspace`：`12K` / `3` 文件
- `.claude`：`20K` / `2` 文件
- `.codex`：`0B` / `0` 文件（仅空目录）

### 高体积文件（>5M）
- `文案部/.venv/.../site-packages/...` 多个二进制文件（最大 38M）
- `公司业务/业务文档/媒体管理系统/GAC媒体评估管理系统简介.pptx`（10M）

### 杂乱信号
- `.DS_Store`：`19` 个
- 备份命名文件（含“备份”）在 `公司业务/业务文档/媒体管理系统/` 内存在 3 个
- 规划文档重复：`task_plan/findings/progress` 分散在多个目录

## 删除建议（按风险分级）

###[P0 可直接删]
- 空目录：`/Users/will/Desktop/通往AGI之路/.codex`（当前仅空 `worktrees`）
- 所有 `.DS_Store` 文件（纯系统噪声）

###[P1 需要你确认后删]
- Gitlink 路径：`/Users/will/Desktop/通往AGI之路/.claude/worktrees/phase3-interaction`
  - 当前状态：已经从磁盘移除，但尚未提交删除。
  - 建议：若后续不再把这个 worktree 作为仓库内容，提交删除。

###[P2 建议重构后删]
- `文案部/.venv`（319M）
  - 建议：从仓库工作目录移出或重建，不纳入版本管理；保留 `requirements/lock` 即可。
- `公司业务/业务文档/媒体管理系统/*备份*.pptx`
  - 建议：保留 1 份最新版，其他归档到独立 `archive/` 或删除。

## 结构调整建议（最小改造）

### 目标结构（建议）
1. `workspace/`：仅放临时分析与执行计划文件。
2. `公司业务/业务文档/`：仅保留“最终版”与“当前生效版”。
3. `文案部/`：作为单独项目区，清理运行时产物（`.venv`、日志）。
4. `archive/`（新建）：集中放历史备份与过期文档。

### 规划文件治理
- 保留根目录一套：`task_plan.md`、`findings.md`、`progress.md`（全局治理）
- 子项目只保留“活跃项目”一套规划文件
- 历史规划统一移至 `archive/plans/`

## 你需要做的决策
1. 是否提交删除 `.claude/worktrees/phase3-interaction`（P1）？
2. 是否删除 `文案部/.venv` 并改为环境重建（P2）？
3. `媒体管理系统` 3 个备份 PPT 是保留 1 个还是全部迁入归档（P2）？

## 推荐执行顺序
1. 先处理 P1（提交 gitlink 删除）  
2. 再处理 P0（清理 `.DS_Store` + 空目录）  
3. 最后处理 P2（`.venv` 与备份文档）
