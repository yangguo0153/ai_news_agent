---
name: content-expansion
description: 批量汽车短视频文案生产工作流。当用户输入创作Brief（含车型、平台、数量、方向）或上传公关部材料，要求批量生成抖音脚本、今日头条稿件时触发。
---

# Content Expansion Skill — Pipeline 编排器

## 触发示例

```
/content-expand 车型CR-V，50篇今日头条，方向春节返乡+家庭用车
```

---

## ⛔ 执行铁律（违反任何一条 = 流程失败）

1. **单批上限** — 每次只处理 1 个批次（头条 3 篇/批，抖音 5 篇/批），禁止一次性输出全部
2. **门禁必停** — 每个 🚫 标记处必须调用 **AskUserQuestion** 等待用户确认，未确认不得继续
3. **审校必过** — Step 5 Reviewer 不可跳过，不可自行判断"质量没问题"而省略
4. **15 篇必交** — 累计 15 篇时必须触发 Step 6 交接，不可在单次对话中超过 15 篇

---

## 架构

```
Brief → Step 1-2 交互 → Step 3 Planner → Step 4 Writer×N 分批 → Step 5 Reviewer → Step 5.5 输出 → Step 6 交接
```

主 Agent 只做**编排调度**，不直接写文案。

---

## 工作流程

### Step 1: 材料消化

1. 解析 Brief：车型、平台、数量、方向
2. 校验种子库 `01-输入材料/既定资料/{车型}.md`，无则停止并提示上传
3. 从种子库提取所有可用**数据点列表**
4. 输出《材料理解摘要》

### Step 2: 需求确认

向用户提问确认：卖点优先级、目标人群、差异化判断。3 题全答才继续。

详见 → `references/tone-guide.md`

### Step 3: 调用 Planner Agent

1. 读取 `references/planner-prompt.md`，注入需求摘要 + 数据点池
2. 使用 **Task tool** 发出 Planner Agent（`subagent_type: general-purpose`）
3. 校验返回 JSON：条数正确、维度均匀（差 ≤ 1）、相邻 5 篇无重复组合、数据点去重（失败重试，最多 2 次）

> 🚫 **门禁**：校验通过后，调用 **AskUserQuestion** 展示分配表摘要（选项：确认继续 / 修改后重新生成）。未确认不得进入 Step 4。

### Step 4: 分批调用 Writer Agent

每个 Writer 只写 1 篇，Writer 之间物理隔离。

**Prompt 组装**（每篇）：

1. 读取 `references/writer-prompt.md` 模板
2. 注入 `{ten_rules}`（← `references/10-rules.md`）
3. 注入 `{style_sample}`（← `02-参考学习/风格语料库/` 下对应平台+风格文件，**轮换选取**：同批次内同风格的多个Writer不得注入相同样本）
4. 注入 `{seed_excerpt}`（← 种子库按 data_point 截取，≤300 字）
5. 注入各维度描述（← `references/diversity-matrix.md`）
6. 注入 `{output_format}`（← 按平台选取格式）
7. 注入 `{opening_samples}`（← 开头变体库中对应 opening 类型，轮换选取2个，同批次不重复）
8. 注入 `{transition_samples}`（← 过渡句库中随机3个，同批次不重复）
9. 注入 `{closing_samples}`（← 结尾变体库中对应 closing 类型，轮换选取2个，同批次不重复）
10. 注入 `{banned_phrases}`（← 本批次已完成文章的：开头首句 + 结尾末句 + 使用的过渡句式）

**批次执行**：

```
BATCH_SIZE = 头条 ? 3 : 5

# 状态追踪（跨批次累积）
used_openings = []
used_transitions = []
used_closings = []
banned_phrases = []

for batch in chunks(分配表, BATCH_SIZE):
    for article in batch:
        # 选取语料时排除已用
        opening_samples = select(开头变体库, type=article.opening, exclude=used_openings, count=2)
        transition_samples = select(过渡句库, exclude=used_transitions, count=3)
        closing_samples = select(结尾变体库, type=article.closing, exclude=used_closings, count=2)

        # 组装 Writer prompt（含负约束）
        prompt = assemble(article, opening_samples, transition_samples, closing_samples, banned_phrases)

        result = Task(Writer, prompt)

        # 更新追踪
        used_openings += opening_samples
        used_transitions += transition_samples
        used_closings += closing_samples
        banned_phrases += extract(result, 首句+末句+过渡句)

    → 🚫 AskUserQuestion: 展示本批次成品（确认下一批 / 修改本批 / 停止）
    if 累计完成 >= 15:
        → 🚫 触发 Step 6 交接，中断批次循环
```

### Step 5: 调用 Reviewer Agent

1. 读取 `references/reviewer-prompt.md`，注入全部成品文章 + 种子库全文
2. 使用 **Task tool** 发出 Reviewer Agent
3. 解析 JSON 质检报告：`status == "返工"` 的文章重新调用 Writer（最多 2 轮）

> 🚫 **门禁**：审校完成后，调用 **AskUserQuestion** 展示审校报告（选项：确认通过 / 查看详情 / 要求返工）。未确认不得进入 Step 5.5。

详见 → `references/quality.md`

### Step 5.5: 确认与输出

生成 Excel。详见 → `references/platform-rules.md`

### Step 6: 上下文管理

> 🚫 **门禁**：累计 15 篇时，输出《任务交接文档》，调用 **AskUserQuestion**（选项：开新对话继续 / 本轮继续（已了解风险））。

详见 → `references/handover-protocol.md`

---

## 六大铁律

1. **不编造** — 信息来自既定资料
2. **不第一人称** — 第三客观视角
3. **不结构化** — 禁用"XX方面/首先/其次"
4. **不广告腔** — 禁用极致/震撼/硬核
5. **不同质** — 每篇组合不同（由 Planner 分配 + Writer 隔离保证）
6. **不车主** — 禁止以车主身份写作，始终保持旁观者/行业观察者视角

---

## 参考文件索引

| 文件                              | 用途                      | 阶段       |
| --------------------------------- | ------------------------- | ---------- |
| `references/planner-prompt.md`    | Planner Agent 提示词模板  | Step 3     |
| `references/writer-prompt.md`     | Writer Agent 提示词模板   | Step 4     |
| `references/reviewer-prompt.md`   | Reviewer Agent 提示词模板 | Step 5     |
| `references/10-rules.md`          | 10 条铁律（注入 Writer）  | Step 4     |
| `references/diversity-matrix.md`  | 7 维模型定义              | Step 3-4   |
| `references/quality.md`           | 完整质量标准              | Step 5     |
| `references/platform-rules.md`    | 平台规范 + Excel 格式     | Step 4-5.5 |
| `references/tone-guide.md`        | 沟通风格 + Step 2 话术    | Step 1-2   |
| `references/title-examples.md`    | 抖音标题参考              | Step 4     |
| `references/handover-protocol.md` | 交接协议                  | Step 6     |

---

_版本：v5.1 硬编码门禁_
_更新：2026-02-08_
